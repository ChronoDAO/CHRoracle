import LoadingComponent from '@/components/loading/loading'

export default function Loading () {
  const searchPlayer = " with the results of your search..."

  return(<LoadingComponent page={searchPlayer} />)
}
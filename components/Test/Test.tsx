

const Test = ({data}) => {
    console.dir(data, { depth: null });
    console.log(data.nft?.item?.categories[0])
    return (
        <div>
            
        </div>
    );
};

export default Test;